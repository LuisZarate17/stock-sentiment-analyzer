# Add screenshots here after running the app.
# Recommended shots:
#   dashboard.png   — full dashboard with TSLA or NVDA loaded
#   bullish.png     — bullish result (green badge)
#   bearish.png     — bearish result (red badge)
#   finbert.png     — same ticker with FinBERT model toggled
